Your job is completed.

ID: <%= job.jobID %>
You can get the report in this mail or view it online at <%= ezBaseURL + '/report/' + job.jobID %>.

Alerts :
<% if (job.alerts.length) { job.alerts.forEach(function (alert) { %>
  - <%= alert %>
<% })} else { %>
  No alert generated.
<% } %>
COUNTER Reports :
<% if (job.counterReports.length) { job.counterReports.forEach(function (reportType) { %>
 - <%= reportType %>: <%= job.headers['Job-Report-' + reportType] %>
<% })} else { %>
  No report generated.
<% } %>
Reject files :
<% job.rejects.forEach(function (reject) { %>
  - <%= reject %> (<%= job.report.get('rejets', 'nb-lines-' + reject) || 0 %>) : <%= job.report.get('rejets', 'url-' + reject) %><% }) %>
