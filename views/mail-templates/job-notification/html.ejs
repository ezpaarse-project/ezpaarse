<html><body style="color: #666666;"><div id="content" style="width: 80%; border: 1px solid #DADADA; padding: 10px; margin: 0 auto; position: relative;">
    <div id="header" style="position: absolute; top: 5px; right: 5px; font-size: 0.9em; color: #979797;">
      ezPAARSE - Job <%= job.jobID %>
    </div>

    <h2>Your job is completed.</h2>
    <p>You can get the report in this mail or <a href="<%= ezBaseURL + '/report/' + job.jobID %>" style="text-decoration: none;">view it online</a>.</p>

    <h3>Alerts :</h3>
    <% if (job.alerts.length) { %>
      <ul>
      <% job.alerts.forEach(function (alert) { %>
        <li><%= alert %></li>
      <% }) %>
      </ul>
    <% } else { %>
      <p>No alert generated.</p>
    <% } %>

    <h3>COUNTER Reports :</h3>
    <% if (job.counterReports.length) { %>
    <ul>
      <% job.counterReports.forEach(function (reportType) { %>
        <li><a href="<%= job.headers['Job-Report-' + reportType] %>" style="text-decoration: none;"><%= reportType %></a></li>
      <% }) %>
    </ul>
    <% } else { %>
      <p>No report generated.</p>
    <% } %>

    <h3>Reject files :</h3>
    <% if (job.rejects.length) { %>
    <ul>
    <% job.rejects.forEach(function (reject) { %>
      <% var nbLines   = job.report.get('rejets', 'nb-lines-' + reject) || 0; %>
      <li><a href="<%= job.report.get('rejets', 'url-' + reject) %>" style="text-decoration: none;"><%= reject %></a> (<%= nbLines %>)</li>
    <% }) %>
    </ul>
    <% } else { %>
      <p>No report generated.</p>
    <% } %>
    Something goes wrong ? You can <a href="mailto:ezpaarse@couperin.org?subject=EzPAARSE%20usage">contact us</a> for any question and join this message in your mail.
  </div>
</body></html>